using System;

static class Source {
	static void WriteMax(int a, int b) {
		if (nodeº == _method) {
			return GetNewMethod(semanticModel);
		}
		else {
			return ComputeNewInvocation(node as InvocationExpressionSyntax);
		}
	}
}