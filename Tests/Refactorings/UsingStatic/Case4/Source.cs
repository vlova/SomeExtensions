using System;
using static System.Console;
using System.Linq;

class Source {
	private static void Ololo() {
		WriteLine(string.Join(", ", Enumerable.Rºange(0, 100)));
		WriteLine(string.Join(", ", Enumerable.Range(0, 100)));
	}
}