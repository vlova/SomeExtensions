// testing transformation to ofType
using System.Collections.Generic;
using System.Linq;

static class Source {
	static string HashOfLowerCaseSequence(string chars) {
		foreach (var @ºchar in chars) {
			var @castedChar = @char as string;
			if (@castedChar == null) continue;
		}
	}
}
